# Master Forge на Amvera (Docker).
# Документация: https://docs.amvera.ru/applications/configuration/docker.html
#
# Приложение слушает порт 3000 (см. Dockerfile). Amvera по умолчанию ожидает 80.

run:
  containerPort: 3000
